import { Contact } from '../types/contacts';

export async function searchLinkedInContacts(query: string): Promise<Contact[]> {
  // TODO: Implement LinkedIn contact search
  // For now, return empty array until we have LinkedIn API integration
  console.log('LinkedIn contact search not yet implemented');
  return [];
}
