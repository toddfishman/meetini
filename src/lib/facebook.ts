import { Contact } from '../types/contacts';

export async function searchFacebookContacts(query: string): Promise<Contact[]> {
  // TODO: Implement Facebook contact search
  // For now, return empty array until we have Facebook API integration
  console.log('Facebook contact search not yet implemented');
  return [];
}
